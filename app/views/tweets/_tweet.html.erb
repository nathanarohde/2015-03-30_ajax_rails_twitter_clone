<%= form_for tweet, remote: true do |f| %>
  <%= tweet.text %>
  <%= link_to '(edit)', edit_tweet_path(tweet), id: 'edit_link', remote: true %>
  <%= link_to '(remove)', tweet, method: :delete, data: {confirm: 'Are you sure?'}, remote: true %>
<% end %>
